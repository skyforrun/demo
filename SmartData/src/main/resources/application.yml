server:
  ssl:
    # 证书位置
    key-store: classpath:server.psf
    # 证书加密方式
    key-store-type: jks
    # 证书加密面膜
    key-store-password: 123456
#    key-alias: server
  port: 443
spring:
  thymeleaf:
    prefix: classpath:/templates/
    check-template-location: true
    suffix: .html
    encoding: UTF-8
    content-type: text/html
    mode: HTML5
    cache: false
  datasource:
    username: postgres
    password: post@123
    url: jdbc:postgresql://192.168.1.245:5432/pg_byne
    driver-class-name: org.postgresql.Driver
  datasource2:
    username: postgres
    password: post@123
    url: jdbc:postgresql://192.168.1.245:5432/pg_3Dscene
    driver-class-name: org.postgresql.Driver
  datasource3:
    username: root
    password: 7495
    url: jdbc:mysql://127.0.0.1:3306/world?serverTimezone=UTC&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
  #ES相关配置
  elasticsearch:
    rest:
      uris: localhost:9200
  redis:
    # Redis数据库索引（默认为0）
    database: 0
    # Redis服务器地址
    host: 39.99.140.108
    # Redis服务器连接端口
    port: 6379
    # Redis服务器连接密码（默认为空）
    password:
    timeout: 10000
  kafka:
    bootstrap-servers: 33.99.140.108:9060,33.99.140.108:9061,33.99.140.108:9062
    consumer:
      group-id: 0
      #key-value序列化与反序列化
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      #每个批次发送消息的数量
      batch-size: 65536
      #缓冲区大小
      buffer-memory: 524288
logging:
  level:
   cn.com.kxyt: debug
xxl:
  job:
    admin:
      addresses: http://39.99.140.108:8080/xxl-job-admin
    executor:
      ip:
      port: 9998
      logpath: /data/xxl-job/jobhandler
      appname: plat-job
    accessToken:
